package com.company.PracticoFactoria;

import java.util.Comparator;

public class BubbleSortSorterImple implements Sorter{
    @Override
    public boolean sort(Object[] arr, Comparator c) {

        /*boolean ordenado = false;
        while (!ordenado)
        {
            ordenado = true;
            for(int i = 0; i<arr.length; i++)
            {
                if(arr[i].compareTo)
            }
        }*/

        return false;
    }
}
